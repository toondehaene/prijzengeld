See `script.py` — main entry point. Run `python script.py` to compute prize allocations (output: `dijlevallei_26_calculated.csv`).
Optional: `scrape.py` and `parse_player_table.py` build the player table if you need to re-fetch rankings.
